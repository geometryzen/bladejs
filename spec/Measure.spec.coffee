describe "Measure", () ->

  it "Construction", () ->
#    quantity = new BLADE.Euclidean3(1, 0, 0, 0, 0, 0, 0, 0)
#    uom = new BLADE.Unit();
#    m = new BLADE.Measure(quantity, 1)
#   expect(d.T.denom).toBe(1)
